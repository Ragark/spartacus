SOV = { 
        annex_country = { target = RUS transfer_troops = yes } country_event = { id = russia.2004 } }
        every_state = { limit = { is_core_of = RUS } SOV = { add_state_core = PREV } }
        every_state = { limit = { is_claimed_by = RUS } SOV = { add_state_claim = PREV } }
        every_state = { limit = { is_core_of = RUS } remove_core_of = RUS }
        every_state = { limit = { is_claimed_by = RUS NOT = { is_core_of = RUS } } remove_claim_by = RUS }
        SOV = { change_tag_from = RUS }
        }
